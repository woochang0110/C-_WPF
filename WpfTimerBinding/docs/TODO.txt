TODO

1. 체크 박스 하나 만들어서 체크 되어 있으면 TWO WAY로 타이머 변하는 값 디스플레이와 입력 받았을 떄 타이머 값 변경 되도록 하고, 체크 해제 되어 있으면 타이머 값 디스플레이 안하고 타이머 값 변경만 하도록 만들어보기

2. 타이머 값이 1000 됬을 떄 다시 0으로 클리어 하게 하기

3. 현재 async를 사용해서 그런지 타이머 증가 속도가 불규칙적임. 1초보다 빠를 때도 있고 느릴 때도 있는 것으로 보임. 해당 증상 개선 필요